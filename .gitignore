# ============================================================
# Este repo versiona SOMENTE o produto open source (o motor).
# O studio/ (dados do operador, os 4 clientes reais) e SAGRADO e PRIVADO:
# e onde se TESTA o produto antes de publicar. NUNCA entra no git.
# O produto anda 1 passo a frente; o que valida no studio vira release.
# ============================================================

# Dados do operador - nunca versionar
studio/

# Segredos (defesa em profundidade, mesmo com studio/ ja ignorado)
.secrets/
**/.secrets/
*.secret
*.pem
*.key
.env
.env.*

# Quarentena (rascunho / em-dev / aposentado) - nunca versionar; expira. Ver CATALOG.md.
_dev/
_drafts/
_retired/

# Saidas geradas / temporarias (regeneraveis - nunca versionar no produto)
release/
.work/
memory/_proposals/
memory/_index/
**/loop-reports/
*.log
*.tmp
Thumbs.db
desktop.ini

# Dependencias (se houver no futuro)
node_modules/
__pycache__/
*.pyc

# LEI da superficie publica (engine/governance/public-surface.md)
# desenvolvimento NUNCA e versionado
brand/landing/
opportunities/
docs/product/
docs/CLAIMS.md
docs/BRAND.md
research/
_drafts/
_dev/
_backups/
_candidatas/
PRODUCT.md
DESIGN-*.md

# TASK-691: vazamento medido no repo publico (13 caminhos nao rastreados e nao ignorados -
# git add -A publicaria tudo, check-public-surface.ps1 nao acusa porque so olha o ja versionado).
# Todos material interno (nenhum e produto); docs/assets/olho-alia.gif segue FORA desta lista de
# proposito - e o unico asset que o README publico referencia (allowlist $docsAssetsAllow em
# package-release.ps1).
docs/decisoes/
docs/brand/
docs/provas/
docs/RELEASE-STATUS.md
docs/DESIGN.md
docs/assets/banner.svg
docs/assets/chart-freio.svg
docs/assets/chart-memoria.svg
docs/assets/chart-verificacoes.svg
engine/governance/edges.json
scripts/extract-secrets.ps1
scripts/migrate-to-studio.ps1
scripts/smoke-test-studio.ps1
